# 项目操作约束

## 小内核重启

- Session Daemon（小内核）默认不得随 App Server（大内核）的发布或重启一起停止、重启或替换。
- 重启小内核前，必须先通过小内核的权威状态确认当前没有由星序托管且仍在运行的会话，包括 `running` 和 `waiting_approval` 状态；不能只依据大内核数据库、前端显示或进程列表判断。
- 只有确认不存在上述会话时，才可以直接重启小内核。
- 如果存在上述会话，必须先取得用户明确同意才能重启小内核；不得以发布、修复、配置更新或“需要使代码生效”为由绕过确认。
- 如果无法读取小内核状态、状态不完整或判断有歧义，按存在运行中会话处理，必须先取得用户明确同意。
- 大内核可以独立重启，但重启脚本必须保持小内核及其托管的 Codex、Hermes、SSH 和 PTY 进程继续运行。
