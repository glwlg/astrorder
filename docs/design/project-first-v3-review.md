# 项目优先侧栏修订验收

- 权威规则：`overlook-parity-and-interactions.md` 的“归属模型与显示层级”。此前 V2 的连接父节点方案已被用户否定。
- 当前侧栏版本：`.stitch/revision-v3.json`，五个页面均通过 get_screen 精确回读，PNG/HTML 下载校验通过。
- 核心截图审查：桌面工作台、移动项目导航已确认项目→会话两级，无连接分区，本机项目无来源标识，远程来源在项目行，会话叶不带来源。
- 其余三个页面是连接列表、连接详情、监控室的侧栏同步修订；其业务主区未要求改动。
- 输入器的分支/Tasks/Todo/Sub-agents 与语音入口保留；生成 HTML 仍有英文功能标签，中文本地化尚需后续统一。
- 生成 HTML 含状态 keyframes，但未包含 prefers-reduced-motion 降级；不得仅凭生成器反馈声称动效可访问性已完成。
- Stitch 建议：收起全部、搜索过滤反馈、移动项目点击反馈；记录为后续交互优化，不自动改为连接分组。
- 本轮未修改前后端业务代码，也未将设计状态冒充真实会话状态。
