# Frontend implementation — 星序 · Astrorder

## User unattended-work instruction
The user is sleeping. Until they return, SKIP any operation requiring approval; record it in the report and continue independent coding/tests. Do not wait on questions, retry blocked operations, bypass approval, use --yolo, or alter security settings. This is NOT blanket consent. Prioritize implementation and safe isolated verification; leave plugin installation, live restarts, credentials, destructive operations and other approval-gated work pending. Clearly distinguish skipped/unverified work from passing checks.

You own frontend/** and docs/reports/frontend.md ONLY. Another Luna session owns backend/** and connectors/** concurrently. Do not modify other paths, shared docs/CONTRACT.md or global settings. If API changes are needed, write proposals in your report. Do not create further coding agents.

Read docs/PRODUCT.md and docs/CONTRACT.md first. Implement the responsive React app using installed Mantine, Tabler icons, Router, TanStack Query, Zustand if needed. Prefer existing accessible components, no custom everything/no single-file UI. Remove unused Vite demo assets/code you replace. Chat, monitor room, Agent settings are distinct routes with shared data. PC sidebar/grid/details and mobile drawer/sheets/cards. Chinese UI, brand 星序 · Astrorder; understated star/constellation identity, readable light/dark styling.

Use contract HTTP and WS. No production mock agents/messages, show honest empty/disconnected/unsupported states and authentication screen. Auth uses HttpOnly session cookie; no credentials in URLs/localStorage. Safe markdown (no raw untrusted HTML). Attachments uploaded via authenticated endpoint; preserve failed drafts. Browser commands via HTTP only, UUID generated once per intentional action. Outbox separate from canonical transcript; stable message/event IDs only, no text/time/order matching. Replay, old snapshot and missing correlation must not lose commands. Disable unsupported controls based on capabilities. Default sticky-bottom follow, genuine browsing pauses, manual return/arrow resumes, async layout not treated as gesture.

Migrate monitor-room user experience by focused read-only reference to P:/workspace/glwlg/ai/Hermes-plugins/overlook desktop plugin and mobile HTML. Never write to that project or installed plugins. Do not inherit DOM scraping/Desktop dependency. Build UI for event-driven tasks/tool activity/approvals and session routing.

Tests first for actual behavioral slices. Include old snapshot after identical second send, different/same attachments, image-only, late ack, reconnect/event replay, route isolation, queue versus accepted state, sticky scrolling, monitor/chat consistency, unauthorized/error handling. Browser tests may intercept API solely in isolated tests and must label this scope. No prompts to real user agents or services. npm run test, lint, build must pass. Add useful Playwright scenarios for integration agent to run against real isolated FastAPI. Do not weaken semantic assertions to pass tests.

Report in docs/reports/frontend.md: actual changes, exact commands/exit codes, API assumptions, unsupported/unimplemented items, responsive browser evidence versus mock-only tests. Do not claim native Agent integration from fixture events. Work to a functioning MVP; if blocked, leave honest disabled UI and explanation, not invented backend responses. Do not commit/push or restart live Desktop/Overlook.
