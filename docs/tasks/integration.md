# Integration assignment — 星序 · Astrorder

## User unattended-work instruction
The user is sleeping. Until they return, SKIP any operation requiring approval; record it in the report and continue independent coding/tests. Do not wait on questions, retry blocked operations, bypass approval, use --yolo, or alter security settings. This is NOT blanket consent. Prioritize implementation and safe isolated verification; leave plugin installation, live restarts, credentials, destructive operations and other approval-gated work pending. Clearly distinguish skipped/unverified work from passing checks.

Run only after both frontend/backend development processes have exited. You are a NEW Hermes session, provider ocx model gpt-5.6-terra reasoning max. Read docs/PRODUCT.md, docs/CONTRACT.md, docs/reports/frontend.md and backend.md (report absent is a blocker, not success). You own cross-stack integration now; no other agents may edit concurrently. Do not start further agents.

1. Inspect actual source and reports, not agent exit codes. Independently run backend pytest/ruff and frontend test/lint/build, discover and run new tests not included in aggregates. Record real outputs and nonzero statuses.
2. Fix mismatches between HTTP schema/auth/cookies/Origin, event subscriptions/replay, message/command identity, queue and capability states. Preserve failed/unknown commands, no text/time/array-position merging. Confirm image-only and repeated identical legitimate sends survive old snapshots/reconnect. Native connector APIs must be documented/source verified; test fixture IDs do not prove native persistence.
3. Exercise a REAL isolated FastAPI server and built frontend with Playwright Chromium desktop and mobile viewports. Use private temp DB, test credentials and inert connector, never real user conversations. Verify login, session/chat/monitor synchronization, attachments, outbox transitions, disconnect/replay, sticky scrolling, no duplicated bubbles, no horizontal overflow, errors/unsupported states. Save evidence/artifacts under .runtime or frontend/test-results and cite exact tests. Test-only fake execution is protocol proof, not Hermes/Codex runtime proof.
4. Verify managed runtime/embedded connector setup independent of Hermes Desktop. Do not reduce acceptance to CLI launch/ACP or use production fake replies. Native support gaps must stay visibly disabled and documented. Never modify installed Hermes/Codex, read secrets, restart live Desktop/relay, install global plugins or send prompts to existing sessions. If native runtime test requires permission, document it instead.
5. Verify production FastAPI serves built SPA and API/WS under one origin safely; configure binding local-only and auth required. Test no arbitrary file read, upload path traversal, unauthenticated WS or role escalation, no shell injection in launch.
6. Update root README with verified startup/test/setup instructions, reconcile docs/CONTRACT.md with implemented protocol, maintain report of limitations. No commits/push. Write docs/reports/integration.md listing verified, failed, unsupported, and user action required; never claim product complete if native connectors remain unverified.

This run is bounded. Prioritize working honest vertical slices and repairing actual regressions, not broad rewrites. At budget end leave precise checkpoint; process exit is NOT an acceptance verdict. Preserve user's existing Overlook and the new branding. Local test server lifecycle belongs to this run only; clean up your own processes.
