# Luna：继续完成剩余功能，不再以有界收尾范围排除缺口
根目录 P:/workspace/glwlg/ai/astrorder。继续原会话20260906_235516_6f65df，gpt-5.6-luna/ocx/max，保留默认300000上下文配置，不改全局配置。

用户最新明确指令：『那让luna继续完成』。上一轮仅修接线的范围限制已结束；现在需要继续补齐报告中的功能缺口与真实验收，而非重新做设计或重复报已有成果。

## 基线与权威
协调者最新独立验证：后端53项pytest+Ruff通过，前端37项test+lint+build通过（包体积warning）。读 docs/reports/stitch-implementation-luna.md 的功能边界、docs/design/overlook-parity-and-interactions.md、project-first-v3-review.md。最新侧栏必须项目→会话两级，各来源项目混排；远程标签仅项目行数量左侧；本机无标签；数据身份按source/profile隔离。禁止恢复连接父节点。

## 本轮实际完成项，按小的可测试垂直切片推进
1. 独立任务详情：运行条点击后台/Todo/子代理后打开桌面面板/移动sheet；显示真实来源会话、状态、进度、命令、可用日志，复制/跳最新/返回，停止必须针对明确任务且有能力校验和确认。补全真实connector公开事件到后端持久化/API/前端订阅路径，不仅静态解析demo；上游不支持的能力显示未报告，不能伪造。
2. 连接运行历史：持久化并展示该稳定连接的部署/启动/断开/失败时间线、阶段诊断，敏感信息脱敏，进程实例不是额外本机连接。详情加载/分页/失败/空态可用。
3. 移动SSH分步流程：原设计是三阶段（基本信息→验证身份→自动部署），不是上一报告误写的“六步”；六类指的是移动页面种类。含新增/编辑/返回保留草稿、已有SSH配置、主机指纹待确认、分阶段自动安装/启动/握手、失败精确定位/重试。信任默认不勾选，不绕过host-key，不读取上传私钥；同一个已有目标设置不能被另一个连接覆盖。
4. 通知与语音完整路径：任务完成/失败/待审批通知默认不擅自授权，权限拒绝/不可用/后台行为清楚；不因重复事件重复通知。语音除已有拒绝/取消外，验证成功录音、停止、预览/编辑、转文字不可用时音频附件回退、只由用户确认发送，取消保留原草稿。区分mock media测试和真实麦克风测试，不擅自录真实音频。
5. 真实SSH与目录完整性：查清旧报告中的native3项目 vs bootstrap12组、重复identity、project-local-only activation边界。代码/隔离旧库回归先完成，不能删除用户真实项目/会话或仅隐藏重复项。已有配置目标允许范围内进行Astrorder插件部署与新隔离会话的握手/目录对照；不向用户既有会话发测试/强制接管，不sudo/改核心/绕过审批。确有访问/信任审批障碍则记录精确阻塞，继续完成其他编码；不能把不尝试当“用户须重新提供环境”。不读取打印凭证。
6. 对照Overlook只读源码，逐项更新功能矩阵：已实现且真实验证/已实现仅fixture验证/上游不支持/环境阻塞/仍缺失。不能把有按钮等同完成全部复刻。

## 验证与交付
- 每条功能先写失败测试再实现，添加跨来源隔离、错误/取消/重连、状态机与reduced-motion回归。
- 最终重跑后端pytest/Ruff、前端test/lint/build；PC和手机浏览器真实点击，fixture与真实后端/SSH证据分别记录。
- 保持现有未提交修改，不reset、不commit/push；Overlook只读，不改Hermes核心、不重启Desktop/Relay、不杀用户进程。
- 不擅自重启30001/30002，测试可在空闲30000+端口启动自有隔离栈，结束只清理自己启动的进程；不使用5173。
- 更新 docs/reports/stitch-implementation-luna.md，开始即列本轮新增待办，每完成一个切片更新证据；每20轮检查剩余预算。最后留足回归与报告时间，不在最后几轮加入未经验证的结构改动。
- 同一问题三次失败停止该路径并记录，不无限补丁。预算到时真实说明剩余项，不声称“范围外”来排除用户已要求的功能。

现在继续编码，不停在计划。优先完成1-4的完整可用流程，再做5的真实验收，已有通过基线无需反复重复审查设计。
