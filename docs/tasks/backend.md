# Backend implementation — 星序 · Astrorder

## User unattended-work instruction
The user is sleeping. Until they return, SKIP any operation requiring approval; record it in the report and continue independent coding/tests. Do not wait on questions, retry blocked operations, bypass approval, use --yolo, or alter security settings. This is NOT blanket consent. Prioritize implementation and safe isolated verification; leave plugin installation, live restarts, credentials, destructive operations and other approval-gated work pending. Clearly distinguish skipped/unverified work from passing checks.

You own backend/**, connectors/** and docs/reports/backend.md ONLY. A Luna frontend agent runs concurrently. Read docs/PRODUCT.md and frozen docs/CONTRACT.md first. Do not change frontend or contract; document proposed additive details for integration. No further coding agents. Do not commit/push, modify installed agents/profiles, or stop/restart Desktop/Overlook relay. Existing Hermes-plugins is READ-ONLY reference.

Implement independent FastAPI control server with installed SQLAlchemy/SQLite, durable commands/events, role auth, session/history/outbox APIs, attachments, Origin validation, safe errors, cursor replay WS, static frontend serving when built. Default local-only 8765, configured secret required for private state (fail closed); use settings/env without reading user's credential files. Tests inject isolated DB/credentials. Separate connector role from browser auth. Browser commands only via HTTP with durable idempotency and mismatch rejection. Unknown submission does not auto retry. Preserve failures/attachments and canonical transcript separate from outbox. No content/time/position based message merging.

Implement Hermes and Codex embedded connectors, not Desktop plugins, stdout scraping, generic CLI chat wrapper or ACP-only proxy. First examine official docs and available local source extension surfaces. Hermes plugin lifecycle should attach bidirectional transport/event callbacks independent of Desktop. Codex hooks/plugin may require a companion. Record exact source/API evidence, supported capabilities and limitations in connectors/README.md. Never invent registration symbols or pretend a passed clientMessageId is persisted without proof. No modifications to installed runtime code or credentials. Provide installable connector artifacts and safe setup instructions instead. Source-compatible connector unit tests and isolation tests required. Where native extension surface truly cannot support command injection/stream/cancel, expose unsupported capability with reason; prioritize one genuine end-to-end supported path over fake adapters.

Server can supervise explicitly owned processes only, fixed executable/argument construction and workspace allowlist, no remote arbitrary shell strings. Runtime launch is not the agent chat transport. Attaching existing instances is distinct from managed launch. No actual prompt submissions in user's conversations. Live real-agent verification must be non-destructive or explicitly reported as unverified; do not invent output. Isolated inert connector is allowed for protocol integration tests, never production fallback.

Test vertical slices RED->GREEN: auth allow/deny, connector role/Origin, duplicate command ID and payload mismatch, old replay/cursor reset, restart persistence, multiple clients, same text/attachments distinct IDs, queue acceptance vs submission, unsupported capabilities, attachment traversal/size, late ack/disconnect unknown, shutdown cancellation. uv run pytest and ruff check . pass. Keep code in modules. Write backend-specific README/config examples with placeholders, no secrets. Report docs/reports/backend.md with exact commands/exit codes, actual native integration evidence, setup needed and remaining blockers. Do not claim all complete when only protocol fake passes.
