# Implement real Hermes connection and local/SSH settings

## Explicit retry and scope clarification
User now requests another attempt in this same session with the same Terra model after context compression. This is the local Hermes/SSH implementation task, NOT the old bootstrap integration task. The old docs/tasks/integration.md no-writers gate does NOT require stopping preview web servers: Python start_preview.py and npm/Vite dev servers are expected running services, not coding agents. Do not block coding on their presence or stale ports 5173/8765/18765. Inspect current 30001/30002 and actual command lines. Preserve running services unless a scoped Astrorder restart is actually necessary. Resume from completed investigation and implement, rather than repeating broad source reads. The user has authorized this retry, not bypassing tool approvals.

## Latest user port override (takes precedence below)
The user reserved 5173 for another project. Coordinator moved Astrorder frontend to http://127.0.0.1:30001 and backend to http://127.0.0.1:30002. NEVER restart Astrorder on 5173. Current Vite proxy and .runtime/start_preview.py reflect these ports; preserve them. Preview backend handle proc_1fd2a19cce91; frontend handle proc_8de56df8e285. Old preview handles below are stopped. Configure native connector endpoint against 30002 and update any dev instructions/tests you touch accordingly. Browser token was regenerated on preview restart; do not print it.

Execution root: P:/workspace/glwlg/ai/astrorder. Resume exact integration session 20260906_235516_6f65df; preserve existing changes. Verify cwd first. Read PRODUCT, CONTRACT, integration and coordinator reports, current connector code. Load hermes-agent and relevant development/testing skills and consult authoritative Hermes docs/source for integration APIs.

Latest user request (supersedes older sleeping-user blanket defer for this scope):
“什么都没有，连Agent设置里都没东西，先接一下Hermes，在前面做联调的会话 20260906_235516_6f65df 会话里完成连接，应该是本地的直接加载，远程的要求我配置，第一版只需要支持ssh方式连接远程”

Deliver implementation, not another inert demo or only a design report:
1. Agent settings must contain a useful local Hermes discovery/load/connect path even with zero connected agents. Local means discover the installed runtime through known paths/executable and safe metadata, then directly load/connect via native plugin/runtime APIs. Do not require users to manually invent WS endpoints/secrets for a same-machine connection. Distinguish discovered, installed, connecting, connected, offline, and errors; do not label metadata/history discovery as live control.
2. Actually connect local Hermes to this Astrorder instance without Desktop integration, DOM scraping, terminal text parsing, or a mere one-shot CLI wrapper. User has authorized scoped local Hermes integration. Prefer per-process plugin loading and environment, or a reversible Astrorder-specific plugin install; do not alter Hermes core, other profiles, model settings, existing conversation content, or restart Desktop/gateway/Overlook. Preserve current layout/theme. If loading into a running process is not supported, launch an owned Hermes runtime via supported native APIs; explain limits honestly. No test prompts to existing user conversations; use a fresh isolated smoke session if needed, do not autoapprove runtime tool actions.
3. Remote v1 supports SSH only. Implement a usable configuration UI and persisted non-secret connection settings (host/port/user, SSH config alias or identity-file reference, remote Hermes path/workspace as appropriate), validation, explicit connect/disconnect/test results, actionable errors. Use system OpenSSH/agent; do not store passwords/private-key contents, disable host-key verification, silently accept changed keys, interpolate user fields into shell commands, or expose unauthenticated remote listeners. SSH transport/tunnel is only transport; it must use the same actual native connector. Do not connect to guessed machines; user has provided no remote host. Request remote configuration via UI. A real remote test remains pending user data, not grounds to fake success.
4. Current preview: frontend Vite 127.0.0.1:5173, backend 127.0.0.1:8765 started by .runtime/start_preview.py (own isolated preview DB). Backend tool handle proc_fc92eeb7da3e, frontend proc_9e88cffcb227. Never assume those launcher PIDs own any other processes; validate command line before stopping. You may restart ONLY these Astrorder preview services as needed to expose changes. Do not read/print .runtime/preview/browser-token.txt or existing credential files. Refactor preview credential lifecycle safely if necessary; generated connector secret currently exists only in preview process environment, so don't scrape process environments. Prefer a controlled preview restart with secure server-managed provisioning. Never print tokens into reports/logs.
5. Implement test-first coverage for discovered empty state, local connection flow, state/error handling, scoped native events/messages and SSH validation/lifecycle/security. Independently run backend pytest/Ruff, frontend test/lint/build and actual local HTTP/browser/native integration. Show a real connected Hermes in the preview Agents page and live native session data where supported. Do not claim all historic sessions or controls work if only newly owned ones do. Stable opaque identity, no content-based deduplication, no automatic resend on unknown outcomes.
6. Bounded work, stop repeated same failure after 3 tries and record root cause/blocker. No commit/push, no extra modifying agents, no approval bypass. Other unrelated approvals remain deferred; if a safety gate blocks, record it and continue other coding.

Finish docs/reports/hermes-local-ssh.md with changed files, actual test commands/results, actual local connection evidence (IDs/status but no secrets), current preview URLs/processes, what remote configuration the user must supply, and remaining limitations. Completing fixtures or exiting 0 is not acceptance. Keep working toward actual local connection, not a promise to connect later.
