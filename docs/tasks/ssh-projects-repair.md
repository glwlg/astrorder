# Fix reproduced SSH deployment and incomplete/duplicate projects
Root P:/workspace/glwlg/ai/astrorder. Continue existing session 20260906_235516_6f65df with default Luna/ocx, config context 300000; do not change model/context/global settings. Preserve all work and navbar click fix. Latest user: plugin automatic deployment fails; sidebar projects incomplete and same project still split. This supersedes unfinished old report task, focus actual red/green regression then live proof.

Confirmed code findings to reproduce BEFORE fixing:
- ssh_transport.py _encoded_script returns raw base64. _install argv passes that directly as python -c argument. It is not executable Python! Bridge path also calls same helper. Need actually decode+exec in Python, AND preserve quoting across OpenSSH remote shell (shell=False on client does NOT solve remote parsing). Exercise generated remote command through actual remote shell, no canned subprocess success mock. Prefer python3/python discovery with correct runtime venv/module and non-interactive PATH discovery. User screenshot remote fields hermes path/identity/workspace are placeholders, not filled values. Read saved non-secret settings, never guess placeholders. Actual configured WSL target is authorized for scoped SSH plugin install; don't disable host-key validation, read keys, use sudo or modify unrelated services.
- _install discards structured stdout error when returncode != 0, producing generic error. Parse safe structured bootstrap failure code/detail first and retain sanitized actionable diagnostics. Do not dump remote output containing secrets. User error must state runtime discovery vs auth vs plugin deployment vs bridge failure.
- Existing plugin layout and runtime Python discovery must be verified against installed remote Hermes API. Idempotent version upgrade should not unconditionally reject our older plugin files, but never overwrite unknown user's files. Verify persisted effect and real handshake.
- SessionRail source key falls back to ephemeral agent_id for legacy records. Thus old owned preview agents form extra identical groups. Add provenance-aware legacy migration/reconciliation with actual local source/session mapping; do NOT collapse same name/path across machines/profiles or delete legitimate sessions. Existing preview DB must migrate safely, not only fresh DB tests. Same source+project should be one group. Preserve opaque stable message/session identity and route mapping. Account for project_id vs workspace fallback consistency.
- Missing projects: paginate_native_session_rows only calls session.list(limit,offset) and treats short page as complete. Check installed native handler for implicit cwd/source/limit/filter restrictions and actual project catalog API; full source should include actual configured-profile projects and sessions, not just cwd Astrorder. Native source IDs/profile boundaries remain distinct. No filtering to owned runtime/current project unless user explicitly chooses it. Respect archived visibility options; do not claim completeness from len<limit without native total semantics. Compare programmatic native catalog counts/IDs with Astrorder view, safe metadata only, no bulk transcript/credential reads. Existing active conversations must not be hijacked or receive test prompts.

Workflow: load systematic-debugging and TDD. Read targeted source only; write RED that executes encoded command through remote shell and real interpreter; legacy DB fixture and native pagination/filter tests. Fix both install and bridge paths. Real configured WSL attempt is authorized for Astrorder plugin deployment and a NEW isolated test session; preserve user's existing sessions. If auth/host-key/runtime prerequisites block, report exact blocker rather than declaring implementation done. Max three identical failures, then stop that avenue; no budget-spinning.

Preview current coordinator restarted: backend handle proc_ef3b5808b660, frontend proc_3e9d27e2b783, ports 30002/30001. Validate actual ownership before scoped restart. Do not alter 5173. Don't read/print .runtime/preview/browser-token.txt or existing credentials. Token lifecycle via owned startup script. Preserve current theme. No Desktop/Overlook restart, no Hermes core changes, no commit/push.

Acceptance: backend full tests/Ruff; frontend test/lint/build; navigation-hit-test; desktop/mobile actual clicks; saved WSL real SSH deploy+native handshake (or exact real environmental blocker); local native source-project-session counts reconcile after migration and page refresh, no repeated same-source project groups. Inspect existing local-session import source filters. Complete docs/reports/ssh-projects-repair.md early with checkpoints and then final evidence, tested counts, live preview state and pending issues. Avoid losing all evidence on upstream disconnect. Do not call screenshots/fixtures alone complete. Previous run never generated final report and user now reproduced actual failure despite tests.
